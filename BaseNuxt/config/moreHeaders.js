// import { xyTools } from 'vue-xy-ui'

// const VERSION = '1.0.0'
// const APPCODE = {
//   'mall': 'MALL'
// }

const moreHeaders = function (headers) {
  // headers.version = VERSION
  //
  // let bs = xyTools.browser()
  // headers.platform = bs.wx ? 'WX_MP' : (bs.type === 'pc' ? 'WEB_PC' : 'WEB_H5')
  //
  // let appCodeKey = window.location.href.match(/#\/([^/]+)/)
  //
  // if (appCodeKey) {
  //   headers.appCode = APPCODE[appCodeKey[1]]
  // } else {
  //   headers.appCode = ''
  // }
}

export default moreHeaders
